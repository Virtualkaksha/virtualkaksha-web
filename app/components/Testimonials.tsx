import SectionTitle from "./ui/SectionTitle";
const safeguards = [["Moderated publishing", "Teacher submissions move through review before becoming publicly discoverable."], ["Protected PDF delivery", "Private storage locations and provider metadata are not exposed to visitors."], ["Account controls", "Authentication, current role checks, session revocation, and rate limiting protect sensitive actions."]];
export default function Testimonials() { return <section className="bg-blue-50 py-20"><div className="mx-auto max-w-7xl px-6"><SectionTitle title="Designed with responsible access" subtitle="Platform safeguards support students, teachers, and moderators without unverifiable success claims." /><div className="grid gap-6 md:grid-cols-3">{safeguards.map(([title, description]) => <article key={title} className="rounded-2xl bg-white p-7 shadow-sm"><h3 className="text-xl font-bold text-slate-900">{title}</h3><p className="mt-3 leading-7 text-slate-600">{description}</p></article>)}</div></div></section>; }
